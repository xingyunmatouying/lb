<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ main_frame.title }}</title>
    <meta name="description" content="lichess bot leaderboard">
    <meta name="keywords" content="lichess bot leaderboard">
    <link rel="stylesheet" href="css/style.css">
  </head>

  <body>
    <header>
      <nav>
        {%- for nav_link in main_frame.nav_links %}
          <a href="{{ nav_link.link }}"{% if nav_link.is_active %} class="active"{% endif %}>{{ nav_link.name }}</a>
        {%- endfor %}
      </nav>
    </header>

    <main>
      {%- block content %}
      {% endblock content %}
    </main>

    <footer>
      🕒Last Updated: {{ main_frame.last_updated_date }} UTC🕘 •
      😸<a href="https://github.com/Eirik0/lichess-bot-leaderboard" target="_blank" rel="noopener noreferrer">Source Code</a>😸
    </footer>
  </body>
</html>
